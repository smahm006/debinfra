enable-ssh-support
pinentry-program /usr/bin/pinentry-gnome3
default-cache-ttl 600
max-cache-ttl 7200
# ssh keys are cached separately from OpenPGP keys; without these the ssh
# push re-prompts independently of the commit signature.
default-cache-ttl-ssh 600
max-cache-ttl-ssh 7200
